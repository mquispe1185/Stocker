<%= form_for(cliente, url: negocio_clientes_path(@negocio), html: {class: "miform form-horizontal"}, remote: true) do |f| %>
  <% if cliente.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cliente.errors.count, "error") %> prohibited this cliente from being saved:</h2>

      <ul>
      <% cliente.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.hidden_field :negocio_id, value: @negocio.id %>
  <div class="row field">
    <%= f.label :nombre, class: "col-sm-4" %>
    <%= f.text_field :nombre, class: "col-sm-4" %>
  </div>

   <div class="row field">
    <%= f.label :direccion, class: "col-sm-4" %>
    <%= f.text_field :direccion, class: "col-sm-4" %>
  </div>
   <div class="row field">
    <%= f.label :telefono, class: "col-sm-4" %>
    <%= f.text_field :telefono, class: "col-sm-4" %>
  </div>
   <div class="row field">
    <%= f.label :email, class: "col-sm-4" %>
    <%= f.text_field :email, class: "col-sm-4" %>
  </div>
 
  
   <div class="actions" align="center">
    <%= f.submit "Crear cliente",class: "btn btn-primary" %>
  </div>

<% end %>
