<%= form_for(caja, url: negocio_cajas_path(negocio_id: @negocio.id), html: {class: "miform form-horizontal"},remote: true) do |f| %>
  <% if caja.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(caja.errors.count, "error") %> prohibited this caja from being saved:</h2>

      <ul>
      <% caja.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="row field">
    <%= f.label :nombre, class: "col-sm-4" %>
    <%= f.text_field :nombre, class: "col-sm-4" %>
  </div>
   <%= f.hidden_field :negocio_id, value: @negocio.id %>

   <div class="actions" align="center">
    <%= f.submit "Crear",class: "btn btn-primary" %>
  </div>
<% end %>





