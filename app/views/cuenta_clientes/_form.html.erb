<%= form_for(cuenta_cliente, url: negocio_cuenta_clientes_path(@negocio), html: {class: "miform form-horizontal"}, remote: true) do |f| %>
  <% if cuenta_cliente.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cuenta_cliente.errors.count, "error") %> prohibited this cuenta_cliente from being saved:</h2>

      <ul>
      <% cuenta_cliente.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
    <div class="form-group">
    <%= f.label :cliente_id, class: "col-sm-4" %>
   <%= f.collection_select :cliente_id, Cliente.where(negocio_id: @negocio.id), :id, :nombre, prompt: "Seleccine cliente", class: "col-sm-5" %>
  </div>
    <%= f.hidden_field :negocio_id, value: @negocio.id %>
  <%= f.hidden_field :monto_adeudado, value: 0 %>
 
  <div class="form-group">
    <%= f.label :observacion, class: "col-sm-4" %>
   <%= f.text_field :observacion, class: "col-sm-5" %>
  </div>
  

   <div class="actions" align="center">
    <%= f.submit "Crear cuenta cliente",class: "btn btn-primary" %>
  </div>

<% end %>
