<%= form_for(detalle_cliente, url:negocio_cuenta_cliente_detalle_clientes_path(@negocio), html: {class: "miform form-horizontal"},remote: true) do |f| %>


  <% if detalle_cliente.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(detalle_cliente.errors.count, "error") %> prohibited this detalle_cliente from being saved:</h2>

      <ul>
      <% detalle_cliente.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

 
  <%= f.hidden_field :cuenta_cliente_id, value: @cuenta.id %>
  <%= f.hidden_field :usuario_id, value: @current_user.id %>
   <div class="form-group">
    <%= f.label :detalle, class: "col-sm-4" %>
   <%= f.text_field :detalle, class: "col-sm-5" %>
  </div>
  <div class="form-group">
    <%= f.label :monto, class: "col-sm-4" %>
   <%= f.text_field :monto, class: "col-sm-5" %>
  </div>
  

   <div class="actions" align="center">
    <%= f.submit "Agregar",class: "btn btn-primary" %>
  </div>


<% end %>
