<h5><%= negocio.admin.nombre %></h5>
<%= form_for(negocio,url: negocio_path(admin_id:negocio.admin.id), html: {class: "miform form-horizontal"}) do |f| %>
  <% if negocio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(negocio.errors.count, "error") %> prohibited this negocio from being saved:</h2>

      <ul>
      <% negocio.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :nombre, class: "col-sm-4"%>
    <%= f.text_field :nombre, class: "col-sm-5" %>
  </div>

  <div class="form-group">
    <%= f.label :direccion, class: "col-sm-4" %>
    <%= f.text_field :direccion, class: "col-sm-5" %>
  </div>

  <div class="form-group">
    <%= f.label :telefono, class: "col-sm-4" %>
    <%= f.text_field :telefono, class: "col-sm-5" %>
  </div>

  <div class="form-group">
    <%= f.label :cuit, class: "col-sm-4" %>
    <%= f.text_field :cuit, class: "col-sm-5" %>
  </div>


  <div class="actions" align="center">
    <%= f.submit "Guardar",class: "btn btn-primary" %>
  </div>
<% end %>