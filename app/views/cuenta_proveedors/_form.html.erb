<%= form_for(cuenta_proveedor, url: negocio_cuenta_proveedors_path(@negocio), html: {class: "miform form-horizontal"},remote: true) do |f| %>
  <% if cuenta_proveedor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cuenta_proveedor.errors.count, "error") %> prohibited this cuenta_proveedor from being saved:</h2>

      <ul>
      <% cuenta_proveedor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :proveedor_id, class: "col-sm-4" %>
   <%= f.collection_select :proveedor_id, Proveedor.where(negocio_id: @negocio.id), :id, :nombre, prompt: "Seleccine proveedor", class: "col-sm-5" %>
  </div>
  <div class="form-group">
    <%= f.label :monto_adeudado, class: "col-sm-4" %>
   <%= f.text_field :monto_adeudado, class: "col-sm-5" %>
  </div>
  <div class="form-group">
    <%= f.label :observacion, class: "col-sm-4" %>
   <%= f.text_field :observacion, class: "col-sm-5" %>
  </div>
  

   <div class="actions" align="center">
    <%= f.submit "Crear cuenta proveedor",class: "btn btn-primary" %>
  </div>

<% end %>
